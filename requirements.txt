# corpus build + card generation (scripts/): python -m venv .venv && .venv/bin/pip install -r requirements.txt
anthropic>=0.60
pydantic>=2
indic_transliteration
# agent + evaluation (app/, eval/): also needs
langgraph>=0.2
sentence-transformers>=3.0,<4
torch
rank_bm25
numpy<2
huggingface_hub>=0.23.2,<1.0
